import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Hide the Next.js dev tools indicator badge that appears in the bottom-left corner.
  devIndicators: false,
};

export default nextConfig;
